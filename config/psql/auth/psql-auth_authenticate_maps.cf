user = {MAIL_PSQL_USER}
password = {MAIL_PSQL_PWD}
hosts = {MAIL_PSQL_HOST}:{MAIL_PSQL_PORT}
dbname = {MAIL_PSQL_DB}
query = SELECT password || E'\x1f' || COALESCE(allow_nets, '') FROM public.authenticate('smtp', '%s', '%{multi_instance_name}') LIMIT 1
