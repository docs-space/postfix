user = {MAIL_PSQL_USER}
password = {MAIL_PSQL_PWD}
hosts = {MAIL_PSQL_HOST}:{MAIL_PSQL_PORT}
dbname = {MAIL_PSQL_DB}
query = select STRING_AGG(mf."Destination", ' ') from public."MailForwarding" mf where mf."Source" = '%s' and mf."Status" = 1;
